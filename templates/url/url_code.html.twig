{% extends 'base.html.twig' %}

{% block title %}Url Info for code {{ url_code.code }}{{ parent() }}{% endblock %}

{% block content %}
    <div class="example-wrapper">
        <h1>Url info {{ url_code.url }} ✅</h1>

        {{ url_code.code }} - {{ url_code.url }} - {{ url_code.count }}
{#         {{ include('url/url_info.html.twig') }}#}
    </div>
{% endblock %}
{#{% block page_name %}<a href="{{ path('short_codes_stats') }}">Url statistics</a> > {{ url_code.code }}{% endblock %}#}